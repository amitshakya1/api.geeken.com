export enum OrderStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    REFUNDED = 'refunded',
    FAILED = 'failed',
    PROCESSING = 'processing',
    ON_HOLD = 'on_hold',
}
